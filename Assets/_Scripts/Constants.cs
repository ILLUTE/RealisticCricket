using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class Constants
{
    // Bowling -->(SPEED)
    public const float MAX_BALLSPEED_V = 160;
    public const float MAX_BALLSPEED_R = 1.6f;
    public const float MIN_BALLSPEED_V = 70;
    public const float MIN_BALLSPEED_R = 1.25f;

    // Bowling -->(SPIN)
    public const float MAX_BALL_ANGLE = 30;
    public const float MIN_BALL_ANGLE = -30;

    // Batting -->(SPEED)
    public const float MAX_BATSPEED_V = 150;
    public const float MAX_BATSPEED_R = 1.8f;

    public const float MIN_BATSPEED_V = 70;
    public const float MIN_BATSPEED_R = 1f;
}
